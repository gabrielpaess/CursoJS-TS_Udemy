//i de index

const frutas = ['Maçã', 'Pêra', 'Uva', 'Banana', 'Abacaxi', 'Limão', 'Melância'];

for (let i = 0; i < frutas.length; i++) {
    //const par = i % 2 === 0 ? 'par' : 'impar';validar seé par ou impar.
    console.log(`Índice ${i}`, frutas[i]);
}